import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Overview" />

<div className="vc-hero">
  <div className="vc-hero-text">
    <h1>Vanilla Components</h1>
    <p>
      Custom element UI primitives and common blocks built for reusable,
      framework-agnostic interfaces.
    </p>
    <div className="vc-hero-meta">
      <span>
        Package: <code>@tonishi0922/vanilla-components</code>
      </span>
      <span>Storybook: web-components</span>
    </div>
  </div>
</div>

<div className="vc-section">
  <h2>Quick start</h2>
  <p>
    Register elements with the <code>define</code> helper, then use the tag in
    your HTML. Import the bundled styles to include design tokens.
  </p>

```ts
import "@tonishi0922/vanilla-components/styles.css";
import { define, UIButton } from "@tonishi0922/vanilla-components";

define("ui-button", UIButton);
```

```html
<ui-button variant="primary">Continue</ui-button>
```
</div>

<div className="vc-section">
  <h2>Component catalog</h2>
  <div className="vc-grid">
    <div className="vc-card">
      <h3>Layout</h3>
      <p className="vc-list">
        <span>UIContainer — <code>ui-container</code></span>
        <span>UILayout — <code>ui-layout</code></span>
        <span>UIStack — <code>ui-stack</code></span>
        <span>UIDivider — <code>ui-divider</code></span>
      </p>
    </div>
    <div className="vc-card">
      <h3>Typography</h3>
      <p className="vc-list">
        <span>UITitle — <code>ui-title</code></span>
        <span>UIText — <code>ui-text</code></span>
      </p>
    </div>
    <div className="vc-card">
      <h3>Form</h3>
      <p className="vc-list">
        <span>UIInput — <code>ui-input</code></span>
        <span>UIButton — <code>ui-button</code></span>
      </p>
    </div>
    <div className="vc-card">
      <h3>Core</h3>
      <p className="vc-list">
        <span>UIImage — <code>ui-image</code></span>
        <span>UICard — <code>ui-card</code></span>
      </p>
    </div>
    <div className="vc-card">
      <h3>Common</h3>
      <p className="vc-list">
        <span>AvatarCard — <code>avatar-card</code></span>
        <span>VideoPlayer — exported class (choose a tag name)</span>
      </p>
    </div>
  </div>
</div>

<div className="vc-section">
  <h2>Design tokens and styles</h2>
  <p>
    Global and alias tokens are exported as CSS files. Use the bundled stylesheet
    or import each token file directly.
  </p>

```css
@import "@tonishi0922/vanilla-components/styles.css";
```

```css
@import "@tonishi0922/vanilla-components/styles/global-tokens.css";
@import "@tonishi0922/vanilla-components/styles/alias-tokens.css";
```
</div>

<div className="vc-section">
  <h2>Repository</h2>
  <p>
    Source and issue tracking live on GitHub:
    <a
      href="https://github.com/tonishi0922/vanilla-components"
      target="_blank"
    >
      github.com/tonishi0922/vanilla-components
    </a>
  </p>
</div>

<style>
  {`
  .vc-hero {
    padding: 32px;
    border-radius: 16px;
    background: linear-gradient(135deg, #f7f7f2 0%, #e6edf6 100%);
    border: 1px solid #e0e6ef;
    margin-bottom: 40px;
  }

  .vc-hero h1 {
    margin: 0 0 8px;
    font-size: 32px;
    letter-spacing: 0.4px;
  }

  .vc-hero p {
    margin: 0 0 16px;
    max-width: 640px;
  }

  .vc-hero-meta {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
    font-size: 14px;
    color: #415169;
  }

  .vc-section {
    margin-bottom: 40px;
  }

  .vc-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 16px;
  }

  .vc-card {
    border: 1px solid #e1e6ee;
    border-radius: 12px;
    padding: 16px;
    background: #ffffff;
  }

  .vc-card h3 {
    margin: 0 0 8px;
  }

  .vc-list {
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .vc-section a {
    margin-left: 4px;
  }

  @media screen and (max-width: 800px) {
    .vc-grid {
      grid-template-columns: 1fr;
    }
  }
  `}
</style>
